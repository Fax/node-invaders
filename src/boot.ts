import { AudioManager } from './audio';
import { ReaderCore } from './readercore';

//const reader = new ReaderCore('new window');

//reader.TestLog();
//reader.TestSdl();

const audiom = new AudioManager();
// audiom.Record();
audiom.Play();

//console.log(SDL2_render.SDL_RenderDrawLine);